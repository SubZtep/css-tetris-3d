// export const calcPercent = (whole: number, amount: number): number => Math.floor((amount / whole) * 100)
export const calcPercent = (whole: number, amount: number): number => (amount / whole) * 100
